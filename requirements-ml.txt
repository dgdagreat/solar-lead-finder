# Optional — only needed to train/run the experimental CNN classifier
# (detector/cnn.py, detector/cnn_train.py). Large download (hundreds of MB).
# The app runs fine without these; the heuristic detector is the default.
-r requirements.txt
torch==2.12.0
torchvision==0.27.0
